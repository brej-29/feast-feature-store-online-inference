fastapi==0.111.0
# Uvicorn constraints:
# - FastAPI 0.111.0:    uvicorn>=0.12.0
# - Gradio 4.44.0:      uvicorn>=0.14.0
# - Feast 0.59.0:       uvicorn>=0.30.6,<=0.34.0
# Choose a version in the intersection: [0.30.6, 0.34.0]
uvicorn[standard]==0.30.6
gradio==4.44.0
# Gradio 4.44.0 only declares huggingface_hub>=0.19.3 (no upper bound), so an
# unpinned install can pick up huggingface_hub>=1.0, which removed HfFolder
# and breaks gradio's import chain (gradio.oauth -> ImportError). Pin below 1.0.
huggingface_hub>=0.24,<1.0
requests==2.32.3
prometheus_client==0.20.0
python-dotenv==1.0.1
pandas==2.2.3
numpy==2.1.3
# Allow Feast to choose compatible versions of pydantic and pyarrow
pyarrow>=21.0.0
scikit-learn==1.5.2
# Postgres online store via Feast (includes psycopg dependency)
feast[postgres]==0.59.0
# Explicit psycopg for local CLI usage (psycopg 3, binary build)
psycopg[binary]>=3.1.0
kafka-python==2.0.2
joblib>=1.3.0
evidently==0.4.36
pandera==0.20.4